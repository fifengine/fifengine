# Rio de Hola Demo — TODO

- [ ] **Tools button** — The console (`F10`) has a "Tools" button that calls `ConsoleExecuter.onToolsClick()`, but the demo only prints `"No tools set up yet"`. Implement actual tools (e.g. instance inspector, coordinate picker, debug overlays). See `eventlistenerbase.onToolsClick`.
