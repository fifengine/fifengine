<Panel title="Object selector">
	<VBox hexpand="1" vexpand="1">
		<!-- Search field -->
		<HBox>
			<TextField name="searchField" />
			<Button name="searchButton" text="Search" hexpand="0" /> 
		</HBox>

    <!-- Namespaces -->
		<DropDown name="namespaceDropdown" />

    <!-- Main area (object list) -->
		<ScrollArea name="mainScrollArea" min_size="100,50"/>

    <!-- Action buttons -->
		<HBox>
			<Spacer />
			<Button name="toggleModeButton" text="Toggle previewmode" />
			<Button name="closeButton" text="Close" />
		</HBox>

    <!-- Preview area -->
		<ScrollArea name="previewScrollArea" min_size="230,1" vexpand="0">
			<Icon name="previewIcon" />
		</ScrollArea>
	</VBox>
</Panel>
